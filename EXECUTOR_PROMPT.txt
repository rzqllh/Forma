You are the executor agent for "Interior Photo Finisher" (working name — rename once confirmed with the owner), a single-user internal tool so the owner's business partner can strip metadata, watermark, resize/compress, and preview-adjust color on interior design photos before delivering them to clients. No AI upscale/enhance in v1 — that's an explicit v2 deferral.

Read AGENTS.md first — it is the source-of-truth router and working rules for this repo. Then read, in this order: DESIGN.md, ARCHITECTURE.md, docs/data-model.md, docs/security.md, docs/testing.md, docs/deployment.md. These are approved planning documents. Treat their Confirmed/Proposed labels as real evidence levels, not as suggestions open for redesign — if you disagree with a Proposed choice, flag it rather than silently overriding it.

BEFORE WRITING ANY IMPLEMENTATION CODE:

1. Draft PROJECT.md and one docs/features/<feature>.md per MVP feature — metadata cleaner, watermark studio (with client/brand presets), resize & compress (social-media size presets, adjustable compression, PNG/JPG conversion), non-destructive color preview (before/after slider, intensity control), batch upload with a concurrency-limited queue, 24-hour soft-delete/restore, and export (single + ZIP). Use docs/feature-spec-template.md as the shape and the six docs above as the only source of facts — do not invent scope beyond what they establish.

2. Cross-check per START-HERE.md Step 4 before starting implementation: every MVP capability has a journey + acceptance criteria; there is exactly one role (Owner, no auth — confirmed, not an oversight); every persisted entity's lifecycle/retention is already defined in docs/data-model.md (including the 24h purge job).

3. If you hit a genuinely blocking gap none of these docs answer, stop and ask. Do not guess at product scope, data model, or security behavior. Low-risk, easily-reversible implementation details (internal file naming, code organization within an already-established pattern) you may decide yourself — record them as Proposed in docs/decisions.md.

IMPLEMENTATION ORDER (matches the dependency order in ARCHITECTURE.md):

1. Scaffold: Next.js (static export) + Tailwind + shadcn/ui, wrangler.toml with a D1 binding and the daily Cron Trigger declared.
2. db/schema.ts (Drizzle) implementing Preset, Batch, and HistoryItem exactly as specified in docs/data-model.md, including the deleted_at soft-delete columns and the FK on-delete behavior (SET NULL for Batch.preset_id, CASCADE for HistoryItem.batch_id).
3. lib/processing/: pure Canvas/OffscreenCanvas functions — metadata strip, resize/compress (with social-media presets + adjustable compression + PNG/JPG/WebP conversion), watermark composite (position/opacity/scale/rotation from a Preset), color-adjustment preview (before/after split-view, non-destructive, intensity slider, reset). Unit-test each against representative fixtures per docs/testing.md.
4. lib/queue/: concurrency-limited job queue (tuned to navigator.hardwareConcurrency) dispatching to a Web Worker pool, with per-job states (queued/processing/done/error) and memory cleanup (revoke object URLs, release canvases) after each job.
5. workers/api/: preset CRUD, signed Cloudinary upload issuance, history read/write, soft-delete + restore endpoints, and the daily purge Cron Trigger handler that hard-deletes rows and Cloudinary assets past the 24h window — every endpoint behind the shared-secret header described in docs/security.md.
6. UI: Library/Upload, Edit panel (operation toggles), Preset Manager, Export — screens and every required state (loading/empty/error/success/disabled/partial-data/destructive-confirmation) exactly as listed in DESIGN.md.
7. Wire it end to end, then walk the Manual Acceptance flow in docs/testing.md (upload a real batch → metadata strip + watermark + resize → export → confirm history → delete a batch → confirm restore inside 24h) before calling v1 done.

Report every command you actually run and its actual result — never claim an unrun check passed (AGENTS.md rule 14). Keep docs/status.md and docs/decisions.md current as you go. If any implementation choice contradicts a "Known Trade-off" or "Open ... Decision" already logged in ARCHITECTURE.md or docs/data-model.md, resolve it against what's logged there rather than re-deciding it from scratch.
